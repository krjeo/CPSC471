import React, { Component } from 'react';

export default class RoomPage extends Component {
    constructor(props){
        super(props);
    }

    render(){
        return <p> This is the RoomPage</p>;
    }
}